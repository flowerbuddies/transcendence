
services:

  app:
    build: .
    ports:
    - "8000:8000"
    depends_on:
      - db
    env_file: .env
    restart: unless-stopped

  db:
    image: postgres:latest
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    env_file: .env
    restart: unless-stopped
